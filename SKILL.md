---
name: server-port-deploy
description: 在 Linux 服务器上部署或更新 Web/API 应用，默认配置 HTTPS 与合适的访问入口，维护中文部署登记表并集中管理应用凭据。用户要求发布项目、更新已有服务、配置域名/端口/HTTPS、避免端口冲突，或整理部署所需 API Key 时使用。
---

# 服务器部署与凭据管理

普通 Web 应用默认采用「用户 → Nginx → 本机后端服务」，按实际协议及既有入口适配，并维护服务器上的中文交接记录。仓库只放工具和模板；真实部署记录、凭据及解密密钥均保存在目标服务器。

用户通常只提供项目、服务器和已有域名。先从当前项目、已授权连接、服务器记录与实际配置获取信息，默认完成文档适配、HTTPS、必要凭据接入、数据保留、验证和中文交付，不要求用户逐项重述。仅在关键权限、凭据或无法推断的要求确实缺失时补问，同时继续独立准备工作；“域名已创建”仍需核验实际 DNS，不当作已验证可用。

## 必须保持的约定

- **中文交付**：`~/server-deployments.md`、`~/server-credentials.md` 的标题、说明、用途、安全措施、备注和最终报告使用中文。文件名、域名、路径、环境变量、项目标识及 JSON 机器字段保持原样。脚本不会自动翻译历史自由文本；迁移时保留原文，核实后再补中文，禁止凭空改写业务含义。
- **先核实再修改**：登记表记录已知状态；端口是否可用还要看实际监听、入口配置和进程。登记表建议不是端口预留。执行部署的授权不代表可以顺便迁移无关应用或凭据。
- **沿用既有入口**：在满足当前要求的前提下，更新时优先保留域名、公共端口、后端端口、服务单元及持久化目录；补齐 HTTPS 等要求确需调整入口时，核验兼容性并记录变更。新服务有域名且可配置 TLS 时优先共享 80/443、按域名路由；没有域名路由时才选择空闲独立公共端口。后端默认绑定 127.0.0.1，端口建议 18001–18999；额外公共端口建议 12001–12999。
- **入口按应用适配**：Nginx 是普通 Web 入口的默认选择。已有其他网关满足相同要求时可沿用；无 HTTP 入口的组件不强加反向代理，特殊协议不能套用 HTTP 草稿。选型与限制见 [入口适配](references/nginx-port-mode.md#入口适配)。用户明确指定的入口要求仍必须满足，无法适配就阻断。
- **HTTPS 默认落实**：公网 Web 入口默认核验 DNS、复用或申请有效证书并检查续期，不等待用户额外要求。确实无法完成时，按 [HTTPS 默认策略](references/nginx-port-mode.md#https-默认策略) 说明原因、范围与后续事项；要求 HTTPS 或承载登录、私密数据、付费模型调用的入口不得降级成公网 HTTP。已有 TLS 网关与本机回环后端按实际链路验证。
- **密钥不进代码和文档**：部署表只记凭据引用，凭据目录只记元数据。禁止把真实值写入 Git、命令行参数、聊天、日志或前端包。密码和 Key 原样保存，不改大小写、不去除空白、不做 Unicode 归一化。新建用户登录密码的认证存储应使用成熟密码哈希；需取回的第三方凭据才进入加密库。
- **明确接入边界**：服务器应用必须通过本技能的启动工具或自己的适配代码读取凭据；systemd、Docker 和 SDK 不会自动解析 `secret://`。本机 Agent 经 SSH 调用服务器工具，服务器应用启动不依赖本机在线。
- **凭据分组不是权限隔离**：project/environment 只组织和选择凭据。同一 Linux 账号下的应用不能靠分组或加密自动隔离；需要应用间隔离时，每个应用使用独立的非 root 账号，限制凭据库、identity、启动器及应用数据的访问权限，并验证实际进程身份。既有共享账号不代表用户接受互相信任；不能满足隔离要求时阻断部署。只有已明确接受互信的场景才使用共享账号，并记录限制，不宣称已隔离。
- **目录简单固定**：新项目默认放在实际部署账号家目录的 `apps/<project>/`，代码直接放入项目目录；root 对应 `/root/apps/<project>/`。先核实账号及家目录，不用本机路径或临时提权后的 `~` 猜测。已有部署沿用核实后的目录，目录规范本身不授权搬迁或删除在线应用。
- **原目录更新**：在固定项目目录更新代码和依赖，不创建 releases、current/previous 链接，不保留多版本代码或设计自动回退。业务数据和上传目录按需保留并排除出代码同步、覆盖及清理范围；凭据库和解密密钥放在应用代码树外。更新失败就停止继续发布，按实际情况修复或报告阻塞。

## 选择本次需要的资料

- 选择入口，或新建/调整 Nginx、HTTPS、WebSocket/SSE：读 [入口与 Nginx 配置](references/nginx-port-mode.md)。
- 首次确定应用目录，或已有应用升级、替换、清理：修改服务器前读 [目录与更新流程](references/update-existing-deployment.md)。
- 涉及 API Key、密码、令牌、共享服务或服务器 AI 应用接入：读 [凭据存储与接入](references/credentials.md)；只在需要凭据时初始化存储。
- 初始化登记表时，使用 [中文登记表模板](references/server-deployments-template.md)。登记工具自动兼容旧英文表头，迁移前备份，保留表外备注；结构无法识别时停止改写，不能初始化覆盖原数据。

## 项目部署文档的适配规则

新部署和更新前，都要查阅项目自带的部署说明及相关启动配置，用于理解依赖、运行方式和不可省略的条件。把这些说明作为核实项目要求的依据，不把其中的命令或建议直接当作执行方案。先确定用户当前明确要求与本技能适用的部署规范；用户明确要求优先于技能默认值，项目文档不自动覆盖它们。

1. **区分硬性要求与默认建议**：结合说明、配置项、启动参数和必要的实现核实，不能把示例值当作不可修改的要求，也不能忽略项目真正依赖的条件。
2. **验证如何适配**：通过项目支持的配置、环境变量、启动参数、反向代理或容器映射适配我方规范。分别核对公共入口、代理监听和内部服务地址，避免把不同层的差异误判为冲突。按改动影响验证依赖、监听、健康检查、回调或业务链路，不能只改登记表就宣称适配成功。
3. **适配成功才部署**：同时满足我方要求和项目硬性条件时，采用适配后的方案，并在中文部署记录中说明文档来源、必要的适配及验证结果。
4. **无法适配就阻断**：存在根本冲突，或缺少证据无法确认关键适配可行时，不启动依赖该方案的线上服务、不修改线上代码、不开放入口。可继续只读检查及独立准备工作；向用户说明项目要求及出处、冲突的我方规则、已尝试或排除的适配方式和证据、需要解决的事项。不得擅自放宽我方要求、照项目原方案部署，或为继续部署而绕开项目硬性条件；有新证据或用户明确调整要求后再重新评估。

## 部署流程

1. **理解项目与当前状态**：按上述适配规则阅读项目部署说明及实际启动配置，确认运行时、构建/启动命令、健康接口、必要环境变量、数据库迁移、持久化路径和并发需求。先明确可行方案或阻断项，再进行后续线上部署操作。检查目标服务器的登记表、监听端口、实际入口与进程管理器。检查配置时筛选必要信息，避免把包含秘密的完整 unit、环境或配置输出到工具日志。
2. **确定入口和访问控制**：核实协议、实际 DNS 与既有入口，默认落实 HTTPS；缺域名时核实可用证书和验证方式，再按实际用途决定入口或受限访问，不能直接认定 HTTPS 不可用。承载登录、私密数据或付费模型调用的公网入口，应在开放前具备 TLS 和相应认证/授权。IP 限制、登录、限流和反机器人措施解决不同问题；反机器人挑战不能代替身份认证。已有授权足够时直接完成必要配置，不重复询问。
3. **落实凭据**：识别每个凭据的项目、环境、用途、类型和应用变量；先查服务器目录及已有配置的位置，不显示值。匹配不明确时不能猜选或覆盖。缺失关键凭据则继续独立准备工作，等待该项信息后再启动依赖它的服务。通过 stdin 入库、显式绑定；按参考文档验证并登记引用。
4. **准备应用**：核实固定项目目录和持久化路径，按项目锁定的依赖安装、构建并更新。需要覆盖运行文件时先停止该应用，可能短暂停机，不承诺无中断。部署账号与应用运行账号分别确认；root 部署不等于应用必须以 root 运行，专用应用账号必须具备所需目录权限。确认后端绑定地址，按实际框架选择并发方式；不得仅靠增加 worker 数判断多用户安全。优先复用既有 systemd/Compose 方式。凭据启动器安装在稳定路径，不能依赖临时技能目录或应用代码更新。
5. **配置并验证入口**：采用 Nginx 时，生成文件只是草稿，未包含应用专属访问控制。先取得可用证书再启用引用它的 TLS 配置，按方案显式使用生成器的 `--https`。修改配置时临时保留原文件；`nginx -t` 通过后 reload，失败还原本次配置改动并核实，验证完成后清理临时副本。采用其他入口时执行对应的校验与生效步骤，不额外安装或重载 Nginx。
6. **验证实际行为**：检查进程状态、后端健康接口、对应域名和协议的实际入口，再从外部检查真实 URL。域名入口的 HTTPS 本机验证要使用正确域名与 SNI，不能用 HTTP 请求 443。核对状态码、预期内容/版本，以及登录或一次最小业务调用；curl 退出 0 或返回登录页都不能单独证明部署成功。无入口组件检查其任务结果或协议本身。限制重试次数；失败按更新参考检查并修复，无法解决就停止发布、说明实际服务状态和阻塞。
7. **登记已确认结果**：更新同一项目/服务器行，写明中文安全状态、凭据引用、固定应用目录、持久化位置、实际版本和验证结果。未使用 Nginx 的 Web 入口用 `upsert --no-nginx` 留空专属字段，在备注记录实际网关及配置位置；项目的无入口组件记入相关部署备注，不虚构端口。未通过验证时记录当前状态与失败结果，不沿用“旧版本正常运行”的未核实结论。由实际部署账号运行登记脚本；root 使用 `--allow-root` 并显式指定目标登记表。

安全检查围绕实际暴露面：检查会返回配置的接口是否泄露 Key，管理/调试接口是否需要关闭或限制，浏览器是否打包了服务端凭据。不要为了部署无条件改写所有业务接口或发明通用密码掩码协议；必要改动遵循项目已有接口约定。

## 常用工具

命令中的脚本路径替换为服务器上实际安装位置，`deploy_home` 设为已核实的部署账号家目录；全局参数放在子命令前。以 root 部署时，下列登记命令均在子命令前加 `--allow-root --registry /root/server-deployments.md`；普通账号临时提权不能把登记表改到 root 家目录。

    python3 scripts/registry.py init
    python3 scripts/registry.py get --project translator --server server-a
    python3 scripts/registry.py find-free --server server-a --start 18001 --end 18999 --used 18001 18002

新增记录须提供访问地址、用户端口和后端端口；更新仅修改显式提供的字段，空字符串可清空可选字段：

    python3 scripts/registry.py upsert --project translator --server server-a \
      --user-url https://translate.example.com --user-port 443 --nginx-port 443 \
      --backend-port 18001 --unit translator.service --app-dir "$deploy_home/apps/translator" \
      --nginx-config /etc/nginx/conf.d/translator.conf --health-check /health \
      --security "HTTPS；应用登录；后台接口限制；敏感配置不返回" \
      --credential-refs translator/prod/dashscope-api-key \
      --notes "实际版本、持久化目录和验证结果以本次核验为准"

`registry.py list` 输出中文 CSV；`list --json` 和 `get` 保留稳定英文 JSON 字段以便 Agent 使用。操作使用锁和原子写入；旧表第一次写入和 `init --force` 之前会生成同目录备份。不要把含有旧秘密的历史文件或备份提交到 Git。

## 最终交付

用中文简要报告实际 URL、部署版本、固定应用目录、进程与配置位置、验证结果、持久化位置，以及两份服务器文档的路径。说明如何参考并适配项目部署文档、实际入口与 HTTPS 状态；存在 HTTP 例外时列出原因和访问范围，被阻断时报告具体冲突和证据，不能写成部署成功。涉及凭据时列出引用、接入方式、实际运行账号及隔离依据，绝不展示值。说明已落实的访问控制和实际剩余事项；尚未验证的外部连通性或服务商 Key 有效性必须标为未验证。CI 通过只说明所列测试在测试环境通过，不能作为已在目标服务器建立权限隔离或完成安全审查的证明。
